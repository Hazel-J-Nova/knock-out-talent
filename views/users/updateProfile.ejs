
<% layout('layouts/boilerplate')%>
<div class="row">
    <h1 class="text-center">Update profile</h1>
    <div class="col-6 offset-3">
        <form action="/admin/newcontent" method="POST" novalidate class="validated-form" enctype="multipart/form-data">

            

            <div class="mb-3">
                <label class="form-label" for="bio">Bio</label>
                <textarea class="form-control" type="text" id="bio" name="cretor[bio]"
                    required></textarea>
                <div class="valid-feedback">
                    Looks good!
                </div>
            </div>
           
                <div class="mb-3">
                    <div class="form-file custom-file">
                        <input type="file" class="form-file-input" id="content" name="content" multiple>
                        <label class="form-file-label" for="image">
                            <span class="form-file-text custom-file-label">avatar</span>
                            <span class="form-file-button">Browse</span>
                        </label>
                    </div>
            </div>
            
            </div> 
            <div class="mb-3">
                <button class="btn btn-success">Update bio</button>
            </div>
        </form>